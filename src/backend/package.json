{
  "name": "merge-wellness-backend",
  "version": "1.0.0",
  "description": "Firebase backend system for Merge Wellness game",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:all": "firebase deploy",
    "emulators": "firebase emulators:start",
    "lint": "eslint . --ext .ts",
    "test": "jest"
  },
  "dependencies": {
    "firebase": "^10.7.1",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "eslint": "^8.54.0",
    "jest": "^29.7.0",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
